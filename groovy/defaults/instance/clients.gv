//
// Prudence Clients
//

import org.restlet.data.Protocol

// Required for use of Directory
componentInstance.clients.add(Protocol.FILE)

// Required for accessing external resources
componentInstance.clients.add(Protocol.HTTP)
